#include <stdio.h>

int main(){

int n,resto,soma;

printf("Digite um número: \n");
scanf("%d",&n);
soma=0;

while (n>0)
{
  resto = n % 10;
  n = (n - resto)/10;
  soma = soma + resto;

}

printf("A soma dos inteiros desse numero é: %d \n",soma);

}
